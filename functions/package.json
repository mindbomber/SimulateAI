{
  "name": "simulateai-firebase-functions",
  "version": "1.0.0",
  "description": "Secure Firebase Functions for SimulateAI Research Platform",
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.8.0",
    "stripe": "^14.25.0"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "echo \"Add your test scripts here\" && exit 0",
    "lint": "echo \"Linting functions...\" && exit 0",
    "build": "echo \"Building functions...\" && exit 0"
  },
  "private": true
}

window.debugScenarioControls = function() { const grid = window.mainGrid; if (!grid) { console.log('MainGrid not found'); return; } const toolbar = grid.scenarioContainer?.querySelector('.scenario-controls-toolbar'); if (!toolbar) { console.log('Toolbar not found'); return; } console.log('Toolbar found:', toolbar); const searchInput = toolbar.querySelector('.search-input'); const filterBtn = toolbar.querySelector('.filter-btn'); const sortBtn = toolbar.querySelector('.sort-btn'); const clearBtn = toolbar.querySelector('.clear-all-btn'); console.log('Components:', { searchInput: !!searchInput, filterBtn: !!filterBtn, sortBtn: !!sortBtn, clearBtn: !!clearBtn }); if (grid.initializeScenarioControls) { console.log('Calling initializeScenarioControls...'); grid.initializeScenarioControls(); } else { console.log('initializeScenarioControls method not found'); } };
